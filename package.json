{
  "name": "cloud-native-inventory-system",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm.cmd run dev --workspace backend\" \"npm.cmd run dev --workspace frontend\"",
    "dev:backend": "npm.cmd run dev --workspace backend",
    "dev:frontend": "npm.cmd run dev --workspace frontend",
    "build": "npm.cmd run build --workspace backend && npm.cmd run build --workspace frontend",
    "start": "npm.cmd run start --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
